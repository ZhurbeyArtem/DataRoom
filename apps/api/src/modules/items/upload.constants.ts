/** Той самий ліміт виставлено й на самому бакеті — сховище не покладається на нас. */
export const MAX_FILE_BYTES = 50 * 1024 * 1024;

export const PDF_MIME = 'application/pdf';

/**
 * Перші байти будь-якого PDF. Саме за ними, а не за заявленим типом,
 * визначається, що лежить у сховищі: Content-Type ставить клієнт, тому
 * mimeType обʼєкта повторює те, що він сам про себе сказав.
 */
export const PDF_SIGNATURE = '%PDF-';
